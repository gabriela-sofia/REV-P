# v1pk — DINO PCA / Cluster Exploratory

## Objetivo

Projetar embeddings válidos review-only em 2D (PCA) e atribuir clusters exploratórios. Com menos de 2 embeddings válidos, saídas vazias com header.

## Cluster não é classe

Os clusters são agrupamentos exploratórios de coerência visual/semântica. `can_be_used_as_class`, `can_create_label` e `can_train_model` são sempre false. Nenhum cluster vira classe operacional ou rótulo.

## Resultado

Embeddings válidos: 0. Linhas PCA: 0. Clusters: 0.
