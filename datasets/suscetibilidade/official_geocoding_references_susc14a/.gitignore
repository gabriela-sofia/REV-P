# SUSC-14A raw acquired reference data is heavy official data and is NOT versioned.
# Reproduce it from manifests/suscetibilidade/susc_14a_official_reference_download_manifest_v1.csv
# (each row carries the source URL, content-type, size and SHA256).
*
!.gitignore
!README.md
