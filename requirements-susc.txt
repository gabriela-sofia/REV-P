# Requisitos da linha causal SUSC (ATIVA). NAO confundir com requirements.txt na
# raiz do repo, que serve a linha DINOv2/embeddings (historica, encerrada como
# candidata a feature -- ver PLANO_ACAO_produto_v1.md).
#
# Requer Python >=3.9,<3.11. Verificado em 05/08/2026: firthlogist e interpret
# nao publicam versao compativel com Python >=3.11 no PyPI. Se voce usa conda,
# prefira environment.yml (ele ja fixa a versao de Python certa); use este
# arquivo so se for mesmo pip/venv puro, com um Python 3.9 ou 3.10 ja ativo.
#
# Instalar:
#   python -m venv .venv-susc
#   source .venv-susc/bin/activate   # Linux/Mac
#   # .\.venv-susc\Scripts\Activate.ps1  # Windows PowerShell
#   pip install -r requirements-susc.txt
#
# Verificar:
#   python -m pytest tests/test_susc_ambiente_treino_setup.py -q

numpy
pandas
scipy
scikit-learn
matplotlib
pytest
pyyaml
tqdm
firthlogist
interpret
